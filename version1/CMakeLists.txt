cmake_minimum_required(VERSION 3.5)

project(hash_all_in_same_directory)

# TODO
# create an executable file using source codes in the directory
# hint : you might need to use add_executable(<Target Name> <Source code> ... )